// Kapyn signature identity tokens — named for Narinder Kapany (father of fiber
// optics). The accent is "fiber-optic light": one warm luminous gold, used as
// the single brand accent (per the design skills' one-accent rule). Replaces
// the multi-color / AI-purple palette on primary chrome.

export const OPTIC = "#E8B25C"; // warm fiber-optic light — the one signature accent
export const OPTIC_BRIGHT = "#F2C879"; // brighter glow / highlights
export const OPTIC_SOFT = "rgba(232, 178, 92, 0.14)"; // fills (buttons, chips)
export const OPTIC_BORDER = "rgba(232, 178, 92, 0.34)"; // hairline borders
